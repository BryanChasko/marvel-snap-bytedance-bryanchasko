# Marvel Snap Cybernetic Loop — Design (Pipeline Overview)\n\nArchitect: Stratia (she/her), stratia-gander-arch\nWorkflow: Design-First (Bryan provided the cybernetic loop architecture; requirements derived from it)\n\nThis is the umbrella design. Individual layer specs live in their own directories.\nSee `.kiro/specs/SPEC-INDEX.md` for the full spec map.\n\n## Execution Substrate\n\nThis pipeline runs on the gander (goose-cli) with coordination from the haunting (kiro-cli).\n\nExisting gander infrastructure we leverage:\n- `goose-proxy.py` — heuristic model dispatcher (add ByteDance models to MODEL_MAP)\n- `prompt-ledger.sh` — CSWR prompt logging (already built)\n- `generate-task-issues.py` — spec-to-GitHub-issue automation (already built)\n- `build-cedar-context.py` — Cedar policy evaluation from git diffs (already built)\n- Cedar policies — no-secrets-in-commits, branch-naming, required-reviews (already built)\n- Qdrant — vector store at localhost:6333 (store game records, semantic search)\n- Valkey — Redis-compatible cache at localhost:6379 (session state, cost tracking)\n- Docker hardening — cap_drop, no-new-privileges, resource limits (already configured)\n- MCP launchers — vision-server, filesystem, github, context7, qdrant (already built)\n- Lead/worker model splitting — expensive model plans, cheap model executes (native goose feature)\n\nReference: https://github.com/BryanChasko/goosecli-heraldstack-gander\n\n## CSWR — Conversation-Scoped Work Reference\n\nThe traceability spine. Every agent action anchored to a CSWR before it does anything.\nReference implementation: `docs/cswr-architecture.md` and `scripts/prompt-ledger.sh` in the gander repo.\n\nCSWR = { issue_id, spec_id, conversation_id }\n\nEnforced at:\n- Layer 0: Prompt Ledger (prompt-ledger.sh)\n- Layer 1: Steering (recipe-level enforcement)\n- Layer 2: Knowledge Bases (Qdrant indexed by spec_id + issue_id)\n- Layer 3: Live Context (runtime signals, PR diffs, CI results)\n- Layer 4: GitHub Integration (branches, PRs, commits carry CSWR)\n\n## Privacy Zones\n\n```\nPRIVATE (local only)          → Layer 1: Ingestion\n  │ sanitized images\n  ▼\nHYBRID (sanitized → vendor)   → Layers 2-4: Classification, Reconstruction, Analysis\n  │ structured JSON\n  ▼\nPUBLIC (anonymized, world-safe) → Layers 5-8: Artifacts, Website, Loop, Narrative\n```\n\nData flows one direction: private → hybrid → public. Never reversed. (Constitution Article II)\n\n## Recipe Architecture\n\nEach pipeline layer maps to a goose-cli recipe or sub-recipe:\n\n| Layer | Recipe | Lead Model | Worker Model | MCP Extensions |\n|-------|--------|-----------|-------------|----------------|\n| 1: Ingestion | `snap-ingest.yaml` | llama3.1:8b | llama3.1:8b | filesystem |\n| 2: Classification | `snap-classify.yaml` | bytedance-vision | llama3.1:8b | vision-server, filesystem |\n| 3: Reconstruction | `snap-reconstruct.yaml` | bytedance-reason | llama3.1:8b | qdrant-shared, filesystem |\n| 4: Analysis | `snap-analyze.yaml` | bytedance-reason | llama3.1:8b | qdrant-shared |\n| 5: Artifacts | `snap-publish.yaml` | llama3.1:8b | llama3.1:8b | github, filesystem |\n| Orchestrator | `snap-pipeline.yaml` | qwen-reason | llama3.1:8b | all of the above |\n\nOrchestrator recipe invokes sub-recipes in sequence, passing CSWR through the chain.\n\n## Model Routing (goose-proxy.py additions)\n\nNew entries for the gander's MODEL_MAP:\n\n| Alias | Provider | Model ID | Use Case |\n|-------|----------|----------|----------|\n| `bytedance-vision` | OpenRouter | `bytedance/ui-tars-1.5-7b` | Screenshot classification, game UI recognition |\n| `bytedance-reason` | OpenRouter | `bytedance/seed-oss-36b-instruct` | Game reconstruction, analysis reasoning |\n\nExisting models used:\n| `llama3.1:8b` | Ollama (local) | Worker model for all layers |\n| `qwen-reason` | OpenRouter | Pipeline orchestrator lead model |\n\n## Qdrant Collections\n\nNew collections for this project:\n\n| Collection | Contents | Indexed By |\n|------------|----------|------------|\n| `snap-game-records` | Structured JSON game records | game_id, deck, outcome, date |\n| `snap-analysis` | Analysis results, matchup data | deck, matchup, metric |\n| `snap-screenshots` | Metadata for processed screenshots | game_id, turn, classification |\n\n## Tracing & Observability\n\n- OTEL spans across all layers, CSWR metadata on every span\n- Jaeger at localhost:16686 for trace querying (gander already runs this)\n- Ralph Wiggum (ralph-wiggum-otel-trace) handles trace analysis\n- CI policy checks via Cedar (extend existing policies)\n- Reference: `docs/otel-tracing-architecture.md` in gander repo\n\n## Website Integration (bryanchasko.com)\n\nGitHub repo: https://github.com/BryanChasko/bryanChasko\nGame records and analysis artifacts published via GitHub PRs.\nWebsite reads from the repo as its data backend.\n